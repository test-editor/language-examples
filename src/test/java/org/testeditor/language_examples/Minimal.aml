package org.testeditor.language_examples

interaction type minimalCallWithoutParmeter {
	template = "minimalcallwithoutparameter"
	method = MinimalFixture.callWithoutParameter()
}

interaction type getBoolean {
    template = "getBoolean"
    method = MinimalFixture.getBoolean()
}

component type MinimalComponentType {
	interactions = minimalCallWithoutParmeter
}

component MinimalComponent is MinimalComponentType {
}
